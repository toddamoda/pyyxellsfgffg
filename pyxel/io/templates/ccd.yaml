
simulation:
  mode: single

  parametric:
    parametric_mode:
    steps:
      -
        enabled: false
        key:
        values:
      -
        enabled: false
        key:
        values:

  calibration:

ccd_detector:

  geometry:

  material:

  environment:

  characteristics:


pipeline:

  # -> photon
  photon_generation: []

  # -> photon
  optics: []

  # -> charge
  charge_generation: []

  # -> charge
  charge_collection: []

  # -> charge
  charge_transfer: []

  # -> signal
  charge_measurement: []

  # -> image
  readout_electronics: []
